from examples.WB.content.get_all_cards import get_all_cards
from examples.WB.marketplace.get_orders import (
    get_orders,
    get_new_orders,
    get_orders_status,
    get_orders_stickers,
)


if __name__ == "__main__":
    # # content
    # cards = get_all_cards()

    # # marketplace
    # orders = get_orders()
    # new_orders = get_new_orders()
    # orders_status = get_orders_status([order.id for order in orders])
    # orders_stickers = get_orders_stickers([order.id for order in orders])

    # supplies
    pass
